<article class="page">
  <header class="page-header">
    <h1><%= page.title %></h1>
    <% if (page.description) { %>
      <p class="page-description"><%= page.description %></p>
    <% } %>
  </header>
  <div class="page-content">
    <%- page.content %>
  </div>
</article>
