# Maintenance Policy

这个博客已经进入稳定维护阶段。后续工作的优先级按以下顺序判断：

1. 内容：优先发布真实项目复盘、专题文章和有长期价值的工程判断。
2. 可靠性：修复构建失败、链接失效、部署异常、性能退化和可访问性问题。
3. 阅读体验：只处理读者真实遇到的搜索、导航、排版和移动端问题。
4. 新功能：必须服务于明确内容或阅读需求，并通过现有审计。

不再因为“页面看起来还能再加点东西”而增加模块。以下改动默认不做：

- 没有真实使用场景的社交、评论或统计组件
- 仅用于装饰的动画、卡片和视觉元素
- 引入外部 CDN 才能运行的前端依赖
- 没有内容承载计划的新栏目

## 发布标准

每篇文章至少应做到：

- 说明问题背景和读者收益
- 给出明确观点、方案或复盘结论
- 包含完整的 description、tags、categories 和 cover
- 正文不是提纲或占位文本
- 能被归入现有分类、专题或项目路径

发布前运行：

```bash
npm run check
npm run check:vercel
```

只有两套部署配置都通过构建和审计，才进入 `main`。
